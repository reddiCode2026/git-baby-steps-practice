# Python Calculator

A small Python calculator project that provides addition and subtraction functions.

## Run

From this directory, run:

```bash
python main.py
```

The program prints example results using the functions in `calculator.py`.
